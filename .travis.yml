language: node_js
node_js:
  - '10'
# Travis-CI Caching
cache:
  directories:
    - node_modules
install: yarn
# 只检测 master 分支的 push
branches:
  only:
    - master
script:
  - npm run test
  - npm run build
  - npm run build:doc
  - npm run deploy
before_deploy:
  - echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > ~/.npmrc
deploy:
  provider: script
  script: 'npm run publish'
  on:
    tags: true
